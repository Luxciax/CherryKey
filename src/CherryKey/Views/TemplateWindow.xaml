<Window x:Class="CherryKey.Views.TemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="自定义复制模板"
        Width="760" Height="560"
        MinWidth="620" MinHeight="460"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="自定义复制模板" FontSize="22" FontWeight="SemiBold"/>
            <TextBlock Margin="0,7,0,14" Foreground="{StaticResource MutedBrush}"
                       Text="可用变量：{{providerName}}、{{providerId}}、{{apiKey}}、{{baseUrl}}、{{modelId}}、{{protocol}}、{{authType}}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="模板" FontWeight="SemiBold" Margin="0,0,0,7"/>
            <TextBox x:Name="TemplateBox" Grid.Row="1" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"
                     FontFamily="Cascadia Mono, Consolas"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="预览" FontWeight="SemiBold" Margin="0,0,0,7"/>
            <TextBox x:Name="PreviewBox" Grid.Row="1" IsReadOnly="True"
                     AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap" FontFamily="Cascadia Mono, Consolas"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"
                    Padding="20,9" Content="关闭" Click="Close_Click"/>
            <Button Style="{StaticResource PrimaryButton}" Padding="20,9"
                    Content="复制结果" Click="Copy_Click"/>
        </StackPanel>
    </Grid>
</Window>
